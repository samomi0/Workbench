services:
  workbench:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
      - PORT=8000
      - DEV_MODE=false
    restart: unless-stopped
